// A physical device cannot reach "localhost" - that resolves to the phone
// itself. When testing on hardware, use your computer's LAN IP instead,
// e.g. http://192.168.1.20:4000
export const API_URL = 'http://localhost:4000';
